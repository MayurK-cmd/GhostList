# Dependencies
node_modules/

# Midnight wallet state (seeds, addresses, balances — sensitive)
.midnight-state.json
.midnight-wallet-state/

# Local dev databases (level-db from running a node)
midnight-level-db/
ghostlist-deploy/midnight-level-db/

# Compiled contract artifacts (generated by `npm run compile`)


# Duplicate deploy directory (kept for reference, not source control)
ghostlist-deploy/

# Screenshots (large binaries, not code)


# Temp scripts / sandbox files
test-path.mjs
verify-root.mjs

# Frontend build output (Nitro SSR)
frontend/.output/
frontend/dist/
frontend/dist-ssr/

# Lovable.dev project config
frontend/.lovable/
frontend/AGENTS.md

# Bun lockfile (project uses npm, not bun)
frontend/bun.lock
frontend/bunfig.toml

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Environment files
.env
.env.local
.env.*.local
frontend/.env
frontend/.env.local

# MCP config
.mcp.json

# OS files
.DS_Store
Thumbs.db
